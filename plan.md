Ah, `.sub-roulette-select` is inside an `advanced-field` which means it's hidden unless `show-advanced` class is on the body!
But wait, we toggled "Modo Avançado" earlier. Did it turn off?
Let me click it again before trying to use it.
